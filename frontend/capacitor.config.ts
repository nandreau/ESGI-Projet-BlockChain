import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Blockchain vehicle repair monitoring',
  webDir: 'www'
};

export default config;
